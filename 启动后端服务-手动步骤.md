# 🚀 启动后端服务 - 手动步骤

## 📋 当前状态

- ✅ 后端代码已部署到服务器：`/var/www/sgc_website/server`
- ✅ Nginx 反向代理已配置
- ⚠️ **需要安装 Node.js 并启动后端服务**

## 🔧 手动安装步骤

### 步骤 1: SSH 连接到服务器

```bash
ssh root@47.106.73.160
```

输入密码：`Botool=300739`

### 步骤 2: 安装 Node.js

服务器使用的是 CentOS/RHEL 系统（使用 dnf），执行以下命令：

```bash
# 安装 NodeSource 仓库
curl -fsSL https://rpm.nodesource.com/setup_20.x | bash -

# 安装 Node.js
dnf install -y nodejs

# 验证安装
node --version
npm --version
```

### 步骤 3: 进入后端目录并安装依赖

```bash
cd /var/www/sgc_website/server

# 安装依赖
npm install --production

# 创建必要的目录
mkdir -p data uploads
```

### 步骤 4: 安装 PM2（推荐，用于进程管理）

```bash
npm install -g pm2
```

### 步骤 5: 启动后端服务

**方式 1: 使用 PM2（推荐）**

```bash
# 启动服务
pm2 start server.js --name sgc-backend

# 保存进程列表（开机自启）
pm2 save

# 查看服务状态
pm2 list

# 查看日志
pm2 logs sgc-backend
```

**方式 2: 直接启动（临时）**

```bash
# 后台运行
nohup node server.js > /tmp/sgc-backend.log 2>&1 &

# 查看日志
tail -f /tmp/sgc-backend.log
```

### 步骤 6: 验证服务

```bash
# 测试后端服务
curl http://localhost:3001/health
# 应该返回: {"status":"ok","message":"Server is running"}

# 测试 API 代理
curl http://localhost/api/experts
# 应该返回专家列表数据（JSON 格式）
```

## ✅ 验证部署

在浏览器中访问：
- **管理后台**: http://47.106.73.160/admin
- **API 测试**: http://47.106.73.160/api/experts

## 📝 PM2 常用命令

```bash
# 查看所有服务
pm2 list

# 查看日志
pm2 logs sgc-backend

# 重启服务
pm2 restart sgc-backend

# 停止服务
pm2 stop sgc-backend

# 删除服务
pm2 delete sgc-backend

# 查看服务详情
pm2 info sgc-backend

# 监控服务
pm2 monit
```

## 🔍 故障排查

### 问题 1: Node.js 安装失败

如果 NodeSource 安装失败，可以尝试：

```bash
# 使用 EPEL 仓库
dnf install -y epel-release
dnf install -y nodejs npm
```

### 问题 2: 端口被占用

```bash
# 检查端口占用
lsof -i :3001

# 或
netstat -tulpn | grep 3001
```

### 问题 3: 服务启动失败

```bash
# 查看错误日志
pm2 logs sgc-backend --err

# 或直接运行查看错误
cd /var/www/sgc_website/server
node server.js
```

### 问题 4: API 返回 502

1. 检查后端服务是否运行：`pm2 list` 或 `ps aux | grep node`
2. 检查后端日志：`pm2 logs sgc-backend`
3. 检查 Nginx 日志：`tail -f /var/log/nginx/sgc_website_error.log`

## 🎯 一键执行脚本（可选）

如果你想一次性执行所有命令，可以在服务器上运行：

```bash
# 安装 Node.js
curl -fsSL https://rpm.nodesource.com/setup_20.x | bash -
dnf install -y nodejs

# 安装 PM2
npm install -g pm2

# 进入后端目录
cd /var/www/sgc_website/server

# 安装依赖
npm install --production

# 创建目录
mkdir -p data uploads

# 启动服务
pm2 start server.js --name sgc-backend
pm2 save

# 验证
sleep 2
curl http://localhost:3001/health
```

---

**完成这些步骤后，后端服务就会启动，管理后台就可以正常使用了！** 🎉




