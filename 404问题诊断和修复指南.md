# 404问题诊断和修复指南

## 概述

本文档提供了诊断和修复404页面问题的完整指南。已创建了三个脚本工具来帮助您检查和修复404相关问题。

## 已修复的问题

✅ **404页面硬编码路径问题**
- 问题：`app/not-found.tsx` 中硬编码了 `/sgc_website/` 路径
- 修复：已改为使用 Next.js 的 `Link` 组件，自动处理 `basePath`
- 状态：已完成

## 诊断工具

### 1. check-404-issue.sh - 全面诊断脚本

**功能：**
- 检查本地构建输出中的404.html文件
- 检查服务器上的404.html文件
- 验证nginx配置
- 测试404页面访问
- 检查basePath配置
- 生成修复建议

**使用方法：**
```bash
./check-404-issue.sh
```

**输出内容：**
- ✅ 本地构建文件状态
- ✅ 服务器文件状态
- ✅ Nginx配置检查
- ✅ 404页面访问测试
- ✅ BasePath配置检查
- ✅ 修复建议

### 2. test-404-page.sh - 404页面功能测试脚本

**功能：**
- 测试多个不存在的URL路径
- 验证HTTP状态码
- 检查404页面内容
- 测试返回首页链接是否工作

**使用方法：**
```bash
./test-404-page.sh
```

**测试场景：**
- 完全不存在的路径
- 嵌套的不存在路径
- 类似真实路径但不存在的页面
- 带查询参数的不存在路径

### 3. fix-404-page.sh - 自动修复脚本

**功能：**
- 检查本地文件配置
- 清理旧构建
- 重新构建项目（使用正确的BASE_PATH）
- 部署404.html到服务器
- 验证部署结果

**使用方法：**
```bash
./fix-404-page.sh
```

**注意：** 需要配置 `deploy.config.sh` 或设置环境变量：
- `DEPLOY_USER` - SSH用户名
- `DEPLOY_HOST` - 服务器地址
- `DEPLOY_PASSWORD` - SSH密码（可选，如果使用SSH密钥）

## 常见问题

### Q1: 为什么会出现404？

**可能的原因：**
1. **访问了不存在的路由** - 用户输入了错误的URL
2. **basePath配置不匹配** - 构建时使用的basePath与部署环境不一致
3. **404.html文件缺失** - 服务器上没有404.html文件
4. **nginx配置问题** - nginx没有正确配置404处理

### Q2: 如何确认basePath配置是否正确？

**检查方法：**
```bash
# 检查next.config.js配置
cat next.config.js | grep basePath

# 检查构建时的环境变量
echo "BASE_PATH=${BASE_PATH:-未设置}"
echo "NODE_ENV=${NODE_ENV:-未设置}"

# 检查构建输出的HTML文件中的路径
grep -r "basePath\|NEXT_PUBLIC_BASE_PATH" out/*.html | head -5
```

**正确的配置：**
- 部署到IP地址：`BASE_PATH= npm run build`
- 部署到GitHub Pages：`NODE_ENV=production npm run build`（使用默认 `/sgc_website`）

### Q3: 404页面显示但链接不工作？

**检查步骤：**
1. 检查404.html中的链接是否正确
2. 运行 `./test-404-page.sh` 测试链接
3. 检查浏览器控制台是否有JavaScript错误
4. 确认Next.js客户端路由已正确初始化

### Q4: 如何手动检查服务器上的404.html？

**SSH到服务器：**
```bash
# 检查文件是否存在
ls -lh /var/www/sgc_website/dist/404.html

# 查看文件内容
head -20 /var/www/sgc_website/dist/404.html

# 检查文件权限
stat /var/www/sgc_website/dist/404.html
```

## 修复步骤

### 快速修复（推荐）

```bash
# 1. 运行诊断脚本
./check-404-issue.sh

# 2. 如果发现问题，运行修复脚本
./fix-404-page.sh

# 3. 运行测试脚本验证
./test-404-page.sh
```

### 手动修复

**步骤1：检查本地文件**
```bash
# 确认not-found.tsx已修复
cat app/not-found.tsx | grep -E "Link|href"
```

**步骤2：重新构建**
```bash
# 清理旧构建
rm -rf out .next

# 使用正确的BASE_PATH构建
BASE_PATH= NEXT_PUBLIC_BASE_PATH= NODE_ENV=production npm run build

# 验证404.html已生成
ls -lh out/404.html
```

**步骤3：部署到服务器**
```bash
# 复制404.html到服务器
scp out/404.html user@server:/var/www/sgc_website/dist/404.html

# 验证文件已部署
ssh user@server "ls -lh /var/www/sgc_website/dist/404.html"
```

**步骤4：测试**
```bash
# 访问不存在的页面
curl -I http://your-server/nonexistent-page

# 应该返回 404 状态码
```

## 验证清单

完成修复后，请验证以下项目：

- [ ] 本地构建生成了 `out/404.html` 文件
- [ ] `404.html` 文件大小合理（通常几KB）
- [ ] `404.html` 包含 "Return to Home" 链接
- [ ] `404.html` 没有硬编码的 `/sgc_website/` 路径
- [ ] 服务器上存在 `/var/www/sgc_website/dist/404.html` 文件
- [ ] Nginx配置包含 `error_page 404 /404.html;`
- [ ] 访问不存在的页面返回404状态码
- [ ] 404页面正确显示
- [ ] 返回首页链接可以正常工作

## 技术细节

### Next.js静态导出和404页面

Next.js在静态导出模式下（`output: 'export'`）会：
1. 从 `app/not-found.tsx` 生成 `404.html`
2. 将 `404.html` 放在构建输出根目录
3. 使用 `basePath` 配置所有资源路径

### Nginx 404处理

当前nginx配置：
```nginx
error_page 404 /404.html;
location / {
    try_files $uri.html $uri /index.html;
}
```

这个配置：
- 当文件不存在时，返回404状态码
- Nginx会查找 `/404.html` 文件
- 如果文件存在，返回404.html的内容
- 如果文件不存在，可能返回默认nginx 404页面

### BasePath处理

Next.js的 `Link` 组件会自动处理 `basePath`：
- 如果设置了 `basePath: '/sgc_website'`，`<Link href="/">` 会生成 `/sgc_website/`
- 如果 `basePath: ''`，`<Link href="/">` 会生成 `/`
- 这确保了404页面的链接始终正确

## 相关文件

- `app/not-found.tsx` - 404页面组件
- `next.config.js` - Next.js配置（包含basePath设置）
- `nginx-main-server.conf` - Nginx服务器配置
- `check-404-issue.sh` - 诊断脚本
- `test-404-page.sh` - 测试脚本
- `fix-404-page.sh` - 修复脚本

## 支持

如果问题仍然存在，请：
1. 运行 `./check-404-issue.sh` 获取详细诊断信息
2. 检查nginx错误日志：`tail -f /var/log/nginx/sgc_website_error.log`
3. 检查浏览器控制台的JavaScript错误
4. 验证构建和部署过程是否成功
